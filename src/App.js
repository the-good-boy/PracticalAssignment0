import PlainTextToCipherText from "./components/PlainTextToCipherText";
function App() {
  //We are returning the component which contains the containers for input and coressponding CipherText 
  return <PlainTextToCipherText/>
}

export default App;
